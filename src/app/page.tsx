import DashboardPage from './dashboard/page';

export default function HomePage() {
  // For simplicity, rendering DashboardPage directly.
  // In a real app, you might use redirect from next/navigation if this page had its own layout or logic.
  return <DashboardPage />;
}
